## 修改项目名称以及包名称

1. 使用 >boot-common(.*)< 全局使用正则搜索过滤，替换为对应的名称
2. 使用 mhd-boot 全局使用正则搜索过滤，替换为对应的名称
3. 替换之后依次修改文件夹名称
4. 对每个模块下的包进行修改
5. 修改启动类中扫描目录 @SpringBootApplication(scanBasePackages = "com.mhd.interview.web")
6. 修改启动类的名称，修改启动类中的日志